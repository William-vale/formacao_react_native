import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    backgroundColor: 'red',
    width: '100%',
    color: 'black',
    fontSize: 20,
    height: 40,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    textAlign: 'center',
  }
});