import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  Title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 23,
    color: '#E5BF3C',
  },
  LogoSize: {
    resizeMode: 'contain', 
    height:120,
    marginTop: 10,
  }
});